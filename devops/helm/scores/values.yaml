tag: latest
hostname: not-set

image: "ghcr.io/falldaysoft/scores/scores:"

# Database migration job (runs as Helm hook)
migrations:
  enabled: true

# Expired scores cleanup cronjob
cleanup:
  enabled: true
  schedule: "0 2 * * *"
